import React from 'react';

const MonitoringStats = ({ alerts = [], correlation }) => { const high = alerts.filter((item) => Number(item.rule?.severity) >= 7).length; const critical = alerts.filter((item) => Number(item.rule?.severity) >= 10).length; const correlated = (correlation?.clusters || []).reduce((total, cluster) => total + Number(cluster.event_count || 0), 0); return <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(150px, 1fr))', gap: 12 }}>{[['EVENTS', alerts.length], ['HIGH', high], ['CRITICAL', critical], ['CORRELATED', correlated]].map(([label, value]) => <div key={label} style={{ background: 'var(--bg-dark)', border: '1px solid var(--border-color)', borderRadius: 'var(--border-radius)', padding: '14px 16px' }}><div style={{ color: 'var(--text-muted)', fontSize: '0.67rem', fontWeight: 700 }}>{label}</div><div style={{ color: 'var(--text-primary)', fontSize: '1.6rem', fontWeight: 700, marginTop: 7 }}>{value}</div></div>)}</div>; };
export default MonitoringStats;
