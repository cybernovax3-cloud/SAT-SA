import React from 'react';
import FindingSeverityBadge from './FindingSeverityBadge';
import FindingDetails from './FindingDetails';

const FindingsTable = ({ findings, selectedId, onSelect }) => <div style={{ display: 'flex', flexDirection: 'column', gap: 10 }}><div style={{ display: 'grid', gridTemplateColumns: '1fr 1.5fr .7fr 1fr .7fr', gap: 12, padding: '0 20px', color: 'var(--text-muted)', fontSize: '0.68rem', fontWeight: 700 }}><span>ID</span><span>FINDING</span><span>SEVERITY</span><span>ENTITY</span><span>RISK SCORE</span></div>{findings.map((finding) => <div key={finding.finding_id || finding.title} style={{ background: 'var(--bg-dark)', border: '1px solid var(--border-color)', borderRadius: 'var(--border-radius)', overflow: 'hidden' }}><button onClick={() => onSelect(selectedId === finding.finding_id ? null : finding.finding_id)} style={{ width: '100%', display: 'grid', gridTemplateColumns: '1fr 1.5fr .7fr 1fr .7fr', gap: 12, alignItems: 'center', textAlign: 'left', padding: '16px 20px', color: 'var(--text-primary)', background: 'transparent', border: 0, cursor: 'pointer' }}><span style={{ fontFamily: 'var(--font-mono)', fontSize: '0.78rem' }}>{finding.finding_id || 'N/A'}</span><span>{finding.title || 'N/A'}</span><FindingSeverityBadge severity={finding.severity} /><span>{finding.affected_entity || 'N/A'}</span><span>{finding.risk_score ?? 'N/A'}</span></button>{selectedId === finding.finding_id && <FindingDetails finding={finding} />}</div>)}</div>;
export default FindingsTable;
