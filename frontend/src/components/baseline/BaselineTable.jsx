import React from 'react';
import { useNavigate } from 'react-router-dom';

const join = (value) => Object.keys(value || {}).join(', ') || 'N/A';
const BaselineTable = ({ entities }) => { const navigate = useNavigate(); return <div style={{ overflowX: 'auto', background: 'var(--bg-dark)', border: '1px solid var(--border-color)', borderRadius: 'var(--border-radius)' }}><table style={{ width: '100%', borderCollapse: 'collapse', textAlign: 'left', fontSize: '0.82rem' }}><thead><tr style={{ borderBottom: '1px solid var(--border-color)', color: 'var(--text-secondary)' }}>{['Agent', 'Agent ID', 'Agent IP', 'Events', 'Severity Distribution', 'Rule Distribution', 'Decoder Distribution'].map((heading) => <th key={heading} style={{ padding: '13px 12px', whiteSpace: 'nowrap' }}>{heading}</th>)}</tr></thead><tbody>{entities.map((entity) => <tr key={entity.agent_id || entity.agent_name} onClick={() => navigate(`/baseline/${encodeURIComponent(entity.agent_id || entity.agent_name || '')}`)} style={{ borderBottom: '1px solid rgba(255,255,255,0.05)', cursor: 'pointer' }}><td style={{ padding: '13px 12px', color: 'var(--primary)', fontWeight: 600 }}>{entity.agent_name || 'N/A'}</td><td style={{ padding: '13px 12px', fontFamily: 'var(--font-mono)' }}>{entity.agent_id || 'N/A'}</td><td style={{ padding: '13px 12px', fontFamily: 'var(--font-mono)' }}>{entity.agent_ip || 'N/A'}</td><td style={{ padding: '13px 12px' }}>{entity.total_events ?? 0}</td><td style={{ padding: '13px 12px' }}>{join(entity.severity_distribution)}</td><td style={{ padding: '13px 12px' }}>{join(entity.rule_distribution)}</td><td style={{ padding: '13px 12px' }}>{join(entity.decoder_distribution)}</td></tr>)}</tbody></table></div>; };
export default BaselineTable;
