import React from 'react';
import Distribution from './SeverityDistribution';
const DecoderDistribution = ({ values }) => <Distribution title="Decoder Distribution" values={values} />;
export default DecoderDistribution;
