import React from 'react';
import Distribution from './SeverityDistribution';
const RuleDistribution = ({ values }) => <Distribution title="Rule Distribution" values={values} />;
export default RuleDistribution;
