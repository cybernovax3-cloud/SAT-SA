import React from 'react';

const EvidenceStats = ({ evidence = [] }) => { const entities = new Set(evidence.map((item) => item.agent?.id || item.agent?.name).filter(Boolean)).size; const rules = new Set(evidence.map((item) => item.rule?.id).filter(Boolean)).size; const high = evidence.filter((item) => Number(item.rule?.severity) >= 7).length; return <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(170px, 1fr))', gap: 16 }}>{[['TOTAL EVIDENCE', evidence.length], ['HIGH SEVERITY', high], ['ENTITIES', entities], ['RULES', rules]].map(([label, value]) => <div key={label} style={{ background: 'var(--bg-dark)', border: '1px solid var(--border-color)', borderRadius: 'var(--border-radius)', padding: '16px 20px' }}><div style={{ color: 'var(--text-muted)', fontSize: '0.68rem', fontWeight: 700 }}>{label}</div><div style={{ color: 'var(--text-primary)', fontSize: '1.8rem', fontWeight: 700, marginTop: 8 }}>{value}</div></div>)}</div>; };
export default EvidenceStats;
