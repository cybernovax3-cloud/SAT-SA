import React from 'react';
import { Search, X } from 'lucide-react';

const AttentionFilters = ({ search, setSearch, onClear }) => <div style={{ display: 'flex', gap: 12, alignItems: 'center', flexWrap: 'wrap', background: 'var(--bg-dark)', border: '1px solid var(--border-color)', borderRadius: 'var(--border-radius)', padding: 16 }}><div style={{ position: 'relative', flex: '1 1 280px' }}><Search size={16} style={{ position: 'absolute', left: 12, top: 10, color: 'var(--text-muted)' }} /><input aria-label="Search attention items" placeholder="Search attention reasons..." value={search} onChange={(event) => setSearch(event.target.value)} style={{ width: '100%', padding: '8px 12px 8px 36px', background: 'var(--bg-darker)', border: '1px solid var(--border-color)', borderRadius: 5, color: 'var(--text-primary)' }} /></div>{search && <button onClick={onClear} style={{ display: 'inline-flex', gap: 5, alignItems: 'center', padding: 8, color: 'var(--danger)', background: 'transparent', border: 0, cursor: 'pointer' }}><X size={14} /> Clear Filters</button>}</div>;
export default AttentionFilters;
