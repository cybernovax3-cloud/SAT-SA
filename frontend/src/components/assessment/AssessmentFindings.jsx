import React from 'react';
import { Link } from 'react-router-dom';

const AssessmentFindings = ({ findings = [] }) => <section style={{ background: 'var(--bg-dark)', border: '1px solid var(--border-color)', borderRadius: 'var(--border-radius)', padding: 20 }}><div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', gap: 12, marginBottom: 14 }}><h2 style={{ fontSize: '0.95rem' }}>KEY FINDINGS</h2><Link to="/findings" style={{ color: 'var(--primary)', fontSize: '0.8rem' }}>View All Findings</Link></div>{findings.length ? <div style={{ display: 'flex', flexDirection: 'column', gap: 10 }}>{findings.map((finding) => <div key={finding.finding_id || finding.title} style={{ border: '1px solid var(--border-color)', borderRadius: 5, padding: 12, background: 'var(--bg-darker)' }}><strong style={{ color: 'var(--text-primary)' }}>{finding.title || 'N/A'}</strong><div style={{ color: 'var(--text-secondary)', fontSize: '0.8rem', marginTop: 5 }}>{finding.affected_entity || 'N/A'} | Severity: {finding.severity || 'N/A'} | Risk: {finding.risk_score ?? 'N/A'}</div></div>)}</div> : <p style={{ color: 'var(--text-secondary)' }}>No findings are currently included in the assessment.</p>}</section>;
export default AssessmentFindings;
