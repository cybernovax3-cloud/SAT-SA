import React from 'react';

const RiskBreakdown = ({ factors = [] }) => <section style={{ background: 'var(--bg-dark)', border: '1px solid var(--border-color)', borderRadius: 'var(--border-radius)', padding: 20 }}><h2 style={{ fontSize: '0.95rem', marginBottom: 14 }}>RISK CONTRIBUTORS</h2>{factors.length ? <div style={{ display: 'flex', flexDirection: 'column', gap: 10 }}>{factors.map((factor, index) => <div key={`${factor.factor || 'factor'}-${index}`} style={{ border: '1px solid var(--border-color)', borderRadius: 5, padding: 14, background: 'var(--bg-darker)' }}><div style={{ display: 'flex', justifyContent: 'space-between', gap: 12, color: 'var(--text-primary)', fontWeight: 600 }}><span>{factor.factor || 'N/A'}</span><span>{factor.score ?? 'N/A'}</span></div><p style={{ color: 'var(--text-secondary)', fontSize: '0.82rem', marginTop: 7, lineHeight: 1.5 }}>{factor.explanation || 'N/A'}</p></div>)}</div> : <p style={{ color: 'var(--text-secondary)' }}>No backend-provided risk contributors are currently available.</p>}</section>;
export default RiskBreakdown;
