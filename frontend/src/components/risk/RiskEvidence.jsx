import React from 'react';

const RiskEvidence = ({ factors = [] }) => <section style={{ background: 'var(--bg-dark)', border: '1px solid var(--border-color)', borderRadius: 'var(--border-radius)', padding: 20 }}><h2 style={{ fontSize: '0.95rem', marginBottom: 14 }}>RISK EVIDENCE REFERENCES</h2>{factors.length ? <div style={{ display: 'flex', flexDirection: 'column', gap: 12 }}>{factors.map((factor, index) => <div key={`${factor.factor || 'factor'}-evidence-${index}`}><strong style={{ color: 'var(--text-primary)', fontSize: '0.82rem' }}>{factor.factor || 'N/A'}</strong><div style={{ display: 'flex', flexWrap: 'wrap', gap: 6, marginTop: 7 }}>{(factor.evidence_references || []).map((reference) => <span key={reference} style={{ color: 'var(--text-secondary)', border: '1px solid var(--border-color)', borderRadius: 4, padding: '3px 6px', fontFamily: 'var(--font-mono)', fontSize: '0.7rem' }}>{reference}</span>)}</div></div>)}</div> : <p style={{ color: 'var(--text-secondary)' }}>No backend-provided evidence references are currently available.</p>}</section>;
export default RiskEvidence;
