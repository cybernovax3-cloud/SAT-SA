import React from 'react';
import { useNavigate } from 'react-router-dom';
import BehaviourStatusBadge from './BehaviourStatusBadge';
import BehaviourReasons from './BehaviourReasons';
const BehaviourTable = ({ entities }) => { const navigate = useNavigate(); return <div style={{ overflowX: 'auto', background: 'var(--bg-dark)', border: '1px solid var(--border-color)', borderRadius: 'var(--border-radius)' }}><table style={{ width: '100%', borderCollapse: 'collapse', textAlign: 'left', fontSize: '0.82rem' }}><thead><tr style={{ color: 'var(--text-secondary)', borderBottom: '1px solid var(--border-color)' }}>{['Agent', 'Agent ID', 'Events', 'Normal', 'Unusual', 'Status', 'Reasons', 'Severity', 'Rule', 'Decoder'].map((heading) => <th key={heading} style={{ padding: 12, whiteSpace: 'nowrap' }}>{heading}</th>)}</tr></thead><tbody>{entities.map((entity) => <tr key={entity.agent_id} onClick={() => navigate(`/behaviour/${encodeURIComponent(entity.agent_id || '')}`)} style={{ borderBottom: '1px solid rgba(255,255,255,0.05)', cursor: 'pointer' }}><td style={{ padding: 12, color: 'var(--primary)', fontWeight: 600 }}>{entity.agent_name || 'N/A'}</td><td style={{ padding: 12, fontFamily: 'var(--font-mono)' }}>{entity.agent_id || 'N/A'}</td><td style={{ padding: 12 }}>{entity.total}</td><td style={{ padding: 12 }}>{entity.normal}</td><td style={{ padding: 12 }}>{entity.unusual}</td><td style={{ padding: 12 }}><BehaviourStatusBadge status={entity.status} /></td><td style={{ padding: 12, maxWidth: 280 }}><BehaviourReasons reasons={entity.reasons} /></td><td style={{ padding: 12 }}>{entity.severityDeviation ? 'Reported' : 'N/A'}</td><td style={{ padding: 12 }}>{entity.ruleDeviation ? 'Reported' : 'N/A'}</td><td style={{ padding: 12 }}>{entity.decoderDeviation ? 'Reported' : 'N/A'}</td></tr>)}</tbody></table></div>; };
export default BehaviourTable;
