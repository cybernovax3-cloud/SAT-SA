import React from 'react';
import { Search, X } from 'lucide-react';
const BehaviourFilters = ({ search, setSearch, status, setStatus, statuses, onClear }) => <div style={{ display: 'flex', gap: 12, flexWrap: 'wrap', alignItems: 'center' }}><div style={{ position: 'relative', flex: '1 1 280px' }}><Search size={16} style={{ position: 'absolute', left: 12, top: '50%', transform: 'translateY(-50%)', color: 'var(--text-muted)' }} /><input placeholder="Search entity..." value={search} onChange={(event) => setSearch(event.target.value)} style={{ width: '100%', padding: '10px 12px 10px 36px', background: 'var(--bg-darker)', color: 'var(--text-primary)', border: '1px solid var(--border-color)', borderRadius: 6 }} /></div><select value={status} onChange={(event) => setStatus(event.target.value)} style={{ padding: 10, background: 'var(--bg-darker)', color: 'var(--text-primary)', border: '1px solid var(--border-color)', borderRadius: 6 }}><option value="All">Status: All</option>{statuses.map((value) => <option key={value} value={value}>{String(value).toUpperCase()}</option>)}</select>{(search || status !== 'All') && <button onClick={onClear} style={{ background: 'transparent', border: 0, color: 'var(--danger)', cursor: 'pointer', display: 'flex', gap: 5, alignItems: 'center' }}><X size={14} /> Clear Filters</button>}</div>;
export default BehaviourFilters;
