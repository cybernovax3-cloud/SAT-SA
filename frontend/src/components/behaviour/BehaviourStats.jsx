import React from 'react';
const BehaviourStats = ({ profiles }) => { const groups = new Map(); (profiles || []).forEach((profile) => { const current = groups.get(profile.agent_id) || { status: 'normal', high: false }; if (profile.behaviour === 'unusual') current.status = 'unusual'; if (profile.reasons?.some((reason) => String(reason).toLowerCase().includes('high-severity'))) current.high = true; groups.set(profile.agent_id, current); }); const values = [['TOTAL ENTITIES', groups.size], ['NORMAL', [...groups.values()].filter((item) => item.status === 'normal').length], ['UNUSUAL', [...groups.values()].filter((item) => item.status === 'unusual').length], ['HIGH SEVERITY ACTIVITY', [...groups.values()].filter((item) => item.high).length]]; return <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(180px, 1fr))', gap: 16 }}>{values.map(([label, value]) => <div key={label} style={{ background: 'var(--bg-dark)', border: '1px solid var(--border-color)', borderRadius: 'var(--border-radius)', padding: '16px 20px' }}><div style={{ color: 'var(--text-muted)', fontSize: '0.68rem', fontWeight: 700 }}>{label}</div><strong style={{ display: 'block', fontSize: '1.75rem', marginTop: 8 }}>{value}</strong></div>)}</div>; };
export default BehaviourStats;
